version: '3.8'

services:
  hookcatch:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - BASE_URL=http://localhost:3000
      - DATABASE_PATH=/app/data/hookcatch.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
